# Memory - 2026-03-21

## Exploration: JWT Authentication with Cookie Storage

### Task (Updated)
User requested: "Implement JWT authentication with access and refresh tokens. The JWT tokens must be set to the client's cookies via backend (HTTP-only, secure cookies). The system should support token refresh, logout, and validation. This will be integrated into the existing Moufube microservices architecture (API Gateway + Auth Service via gRPC)."

### Key Findings
1. **Comprehensive architecture plan already exists** at `.opencode/plan/20260214_185528_implement-jwt-authentication-cookie.md`
   - 41KB detailed plan with all specifications
   - Covers security, data flows, implementation phases

2. **Proto definitions already updated**
   - `login/response.proto` has `access_token` and `refresh_token` fields
   - New RPC methods defined: `Refresh`, `Logout`, `Validate`
   - Proto files exist in `data/proto/authentication/v1/token/`

3. **No JWT implementation exists yet**
   - Greenfield implementation
   - No refactoring needed
   - Can follow existing patterns exactly

4. **Existing patterns to replicate**:
   - Cookie setting: `services/api-gateway/internal/bootstrap/middleware/identity.go`
   - Middleware pattern: Same file
   - Use case pattern: `services/authentication/internal/application/usecase/user/login.go`
   - Repository pattern: `services/authentication/internal/domain/repository/user/`

5. **Frontend is minimal**
   - Only basic Next.js structure exists
   - Auth UI needs to be built from scratch
   - Can proceed independently after backend complete

### Architecture
- **Backend**: Go microservices (API Gateway + Authentication Service)
- **Frontend**: Next.js 15.5.4 + React 19 + TypeScript
- **Communication**: gRPC between services
- **Databases**: PostgreSQL (auth), Redis (cache/tokens)

### Key Files to Create
1. `services/authentication/internal/domain/service/jwt.go` - JWT service
2. `services/authentication/internal/domain/repository/token/` - Token repository interfaces
3. `services/authentication/internal/infrastructure/repository/token/redis.go` - Redis implementation
4. `services/authentication/internal/application/usecase/user/refresh.go` - Refresh use case
5. `services/authentication/internal/application/usecase/user/logout.go` - Logout use case
6. `services/authentication/internal/application/usecase/user/validate.go` - Validate use case
7. `services/api-gateway/internal/bootstrap/middleware/jwt.go` - JWT middleware
8. `services/api-gateway/internal/modules/authentication/refresh/` - Refresh module
9. `services/api-gateway/internal/modules/authentication/logout/` - Logout module

### Key Files to Modify
1. `services/authentication/internal/application/usecase/user/login.go` - Add JWT generation
2. `services/api-gateway/internal/modules/authentication/login/controller.go` - Set cookies
3. `services/api-gateway/internal/bootstrap/middleware/constant/cookie.go` - Add token constants
4. `services/authentication/internal/config/type.go` - Add JWT config
5. `services/api-gateway/internal/config/type.go` - Add Redis token DB config

### Dependencies to Add
- Backend: `github.com/golang-jwt/jwt/v5` for JWT operations
- Frontend: No additional dependencies needed (HTTP-only cookies handled by browser)

### Cookie Configuration
- `ACCESS_TOKEN`: 15 min expiration, HttpOnly, Secure, SameSite=Strict
- `REFRESH_TOKEN`: 7 days expiration, HttpOnly, Secure, SameSite=Strict

### Redis Configuration
- Token blacklist in Redis DB 1 (separate from identity DB)
- Key patterns: `token:blacklist:{jti}`, `token:refresh:{user_id}:{jti}`

### Output
Created comprehensive exploration report at `../project-context/context.md` (50KB)
- Complete file inventory with status (exists/needs modification/new)
- Detailed folder structures for all services
- Key dependencies (existing + new)
- Entry points for all services
- Existing patterns with full code examples
- Technical specifications (JWT config, cookie settings, Redis keys)
- Complete data flows (login, refresh, logout, protected routes)
- 9-phase implementation plan with file lists
- Security considerations (token, cookie, infrastructure)
- Environment variables (new additions)
- gRPC service methods (proto definitions)
- API endpoints (planned)

### Next Steps
Backend implementation (Authentication Service + API Gateway) → Frontend integration

### Complexity
Large (L) - Multiple services, new components, Redis integration, middleware implementation
